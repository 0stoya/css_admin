# CSS Admin

Headless Admin application for the Fluid / CSS Commerce Magento backend.

Application delivery is tracked independently from `0stoya/Fluid`; the backend repository remains the authoritative source for GraphQL schema, compatibility contracts and staging acceptance evidence.
